import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nguoianphu.pkmn',
  appName: 'pkmn',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
